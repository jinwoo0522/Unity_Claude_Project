## 킬 스코어 판 만들기

#개요
Tab을 누르고 있으면 (유지 되면) 킬, 데스 , 가한 데미지가 표시되는 UI를 만듭니다.

#내용
플레이어 한명 당 한 명씩 UI가 표시됩니다.
화면 중앙에 반투명 Panel 안에 UI Object를 구성하는 방식으로 합니다.
아마 UI 프리펩을 생성하고 위치를 판정하는 시스템을 만듭니다.
UI Object는 텍스트로 이름 , 킬 , 데스 , 가한 데미지를 표시합니다.

#관리
게임 매니저에 ScoreManger를 추가해야합니다.
다른 Manager처럼 순수 클래스로 생성한 후 게임매니저를 통해 사용할 수 있도록 구현합니다.
각각의 플레이어는 데미지를 줄 때, 죽일 때, 죽을 때를 모두 ScoreManger를 통해 카운팅합니다.
데이터 구조체를 만들어서 플레이어마다 관리하도록합니다.
클라이언트 번호를 통해 식별합니다.
플레이어가 생성되면 ScoreManger에 데이터 구조체를 추가하고 관리합니다.

#죽음
현재 죽는다 와 죽인다라는 판정이 없습니다.
일단 플레이어의 체력이 0이되면 Die함수를 부르고 카운팅만 수행하세요


# 정렬
각각의 플레이어 UI 오브젝트는 킬 수에 따라 정렬되도록 구현할 것입니다.
킬이 발생하면 해당 UI 오브젝트를 담고 있는 리스트를 정렬합니다.
정렬되면 UI오브젝트의 위치를 다시 잡습니다.
절대 Update에서 UI의 위치를 구성하지 않습니다.

#질문
궁금한 사항이 있거나 설계에 오류가 존재한다면 알림을 날려주세요
