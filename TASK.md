#상태 효과 합치기, 빙결 구현하기
@StatusEffect_Airborne.cs 과 @StatusEffect_Slow

현재 상태 효과들이 전부 컴포넌트로 분리되어 있습니다.
상태 이상은 플레이어마다 상태이상을 관리하는 하나의 컴포넌트가 관리합니다.
 

상태 이상 클래스들은 StatusEffect라는 추상클래스 아래에 자식으로 Status가 구현되도록 합니다.
분리되어 있는 기능은 여러 개의 순수 C# 클래스로 구성합니다.
그 여러개의 상태 이상 객체를 모두 가지고 있는 Player_Status라는 컴포넌트가 플레이어에 붙어 상태 이상 효과를 플레이어에게 부여합니다.


빙결은 현재 없으니 새로 만듭니다.
빙결은 Magician의 Q스킬인 Ice_Explosion이 부여합니다.
빙결은 2초간 플레이어가 움직이지 못합니다.
공격, 점프 ,이동 모든 이동이 불가합니다. 
하지만 중력은 받습니다. 점프 상태에서 빙결 당하면 언 상태로 바닥으로 떨어집니다.

코루틴 사용은 순수 C#클래스가 수행할 수 없으니 Player_Status 클래스가 대신 수행하고 호출하도록 합니다.
