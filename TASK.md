## Day6 Task

# 두 번째 플레이어 이동 구현하기
- 씬에 Player2 라는 객체를 통해 만들 것이다. 내부에 프리펩이 존재한다
- 전에 만들었던 Player와 동일하게 만들 것인데 애니메이션은 GolemAni 파일에 들어 있다.
- 공격의 경우는 다르게 구현할 것이므로 일단 애니메이션만 출력되게 만든다 ( 공격 이펙트가 안나가도 된다 ) 공격의 경우 하위 자식에서 오버라이딩 하도록 구현한다
- 좌,우 뛰는 애니메이션이 존재하지 않는다. 따라서 걷는 애니메이션을 속도를 조금 더 빠르게 하여 사용하도록 한다.
- Player Skriptable Object를 하나 더 생성하여 플레이어마다 존재하도록 하며 기본 값은 이 값을 참조하여 Stat 반영하도록 한다.
- 이후 서버를 붙여 네트워크 상에서 할 것이므로 기존 플레이어를 visible false 동일한 키입력으로 구현한다.
- 기존의 Player 관련 스크립트들을 상위 스크립트로 만들고 상속받아서 자식 스크립트에서 재구현 하는 식으로 바꿔야 할 것 같다.
- 그러면 기존 플레이어도 Magician_Move 이런 식으로 스크립트를 받아서 상위 스크립트에게 상속받아서 구현되는 식으로 
- 아바타 마스크는 일단 기존 것을 쓰도록 한다.
-  Cotroller도 기존 Player 것을 참조하여 하나 더 생성한다.  
	
## 추가 Task
# 애니메이션 Idle 상태 다시 잡기
- 현재 이동 애니메이션이 어떤 상황에서도 반드시 idle을 거치도록 구현이 되어있다.
- idle을 기본 애니메이션으로 만든 후 LocoMotion을 다른 상태로 만든다.
- 움직이는 상태에서도 Jump가 발생하고 Idle 상태에서도 점프가 발생해야한다 
- 움직이는 상태에서 Jump 혹은 Hit시 Idle 상태를 거치지 않고 바로 점프가 나오도록 해야한다.
- 최대한 자연스러운 움직임이 연출될 수 있도록 한다.
