골렘의 근접 공격 시스템을 구현합니다.

## 콜라이더 설정
- 골렘 손 뼈에 부착된 무기 오브젝트에 IsTrigger 콜라이더 추가
- 크기는 무기보다 약간 크게 설정

## 활성화 타이밍
- 공격 애니메이션 시작 시 AnimationEvent로 콜라이더 활성화
- 공격 애니메이션 종료 시 AnimationEvent로 콜라이더 비활성화

## 충돌 판정
- 충돌 대상: "Player" 태그를 가진 오브젝트 중 자기 자신 제외
- 공격 1회당 동일 객체에 대한 충돌은 최초 1회만 처리 (히트 목록으로 관리)
- 히트 목록은 공격 시작(콜라이더 활성화) 시점에 초기화

## 데미지 처리 (서버 권위적)
- 클라이언트: OnTriggerEnter에서 충돌 감지 후 서버 RPC 호출
- 서버: RPC 수신 후 피격 대상의 스탯 컴포넌트에서 HP 차감 (공격자의 스탯 컴포넌트 공격력 참조)
- HP는 네트워크 변수로 동기화

## 넉백 처리
- 충돌 방향(공격자 → 피격자)으로 넉백 적용
- 넉백 강도는 Inspector에서 조절 가능한 변수로 노출
- Transform은 자동 동기화되므로 피격 클라이언트에서 직접 Rigidbody에 force 적용
- 서버에서 피격 클라이언트로 넉백 방향과 강도를 ClientRpc로 전달
