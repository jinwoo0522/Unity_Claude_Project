# AGENTS.md

이 파일은 이 프로젝트에서 Codex가 따라야 하는 프로젝트 전용 작업 지침이다.

## 우선 문서

- 모든 작업 시작 전에 프로젝트 루트의 `TASK.md`를 먼저 읽고 따른다.
- 사용자의 직접 요청이 이 문서보다 우선한다.
- 여러 지침이 충돌하면 더 구체적이고 현재 작업에 가까운 지침을 우선한다.

## 언어 및 환경

- Unity Editor 관련 상호작용은 항상 `unity-cli`를 사용한다.
- `unity-cli` 설치 경로: `C:/Users/kim05/AppData/Local/unity-cli/unity-cli`

## Unity 작업 기본 명령

- 스크립트 재컴파일: `unity-cli editor refresh --compile`
- 콘솔 에러 확인: `unity-cli console --type error`
- 플레이 모드 진입: `unity-cli editor play`
- 플레이 모드 종료: `unity-cli editor stop`
- 에디터 상태 확인: `unity-cli status`

## 작업 절차

- 코드 변경 전 기존 구조와 관련 파일을 먼저 확인한다.
- 변경 범위가 크면 작업 전에 짧은 계획을 먼저 공유한다.
- 작업이 독립적인 하위 작업으로 분리 가능하고 서브 에이전트 사용이 효율적이라고 판단되면, 자동으로 분할하지 말고 먼저 사용자에게 제안한 뒤 승인을 받아 진행한다.
- 문제를 수정할 때는 재현 원인과 수정 내용을 분리해서 설명한다.
- 목적에 필요한 최소 변경을 우선하고, 불필요한 대규모 수정이나 스타일 변경은 피한다.
- 가능하면 변경 후 관련 검증을 수행한다.
- 실행하지 못한 검증이 있으면 이유를 명확히 밝힌다.
- 코드에 간단한 주석을 반드시 작성한다.

## Unity 에디터 및 설정 변경 규칙

- 코드 변경과 Unity 에디터 설정 변경 전에는 먼저 사용자 승인을 받는다.
- `Assets/Reimport All`은 절대 실행하지 않는다.

## 코드 작성 규칙

- Inspector에서 값을 받아야 하는 필드는 `public` 대신 `[SerializeField] private` 또는 `[SerializeField] protected`를 사용한다.
- 스크립트 내부 상태를 표현하는 `public` 변수 사용을 지양하고 은닉화를 우선한다.
- 기존 구조를 훼손하지 않는 범위에서 상속, 공통화, 재사용 가능한 구조를 우선 검토한다.
- 나눌 수 있는 기능은 가능한 모듈화 한다.
- 관련된 코드가 10줄 이상이거나 함수화 하기에 구조가 좋다고 생각하면 기능을 함수화 하도록 작성한다.

## 에셋 참조 규칙

- 애니메이션, 프리팹, 마스크 등 에셋을 참조할 때는 반드시 현재 프로젝트의 실제 파일과 guid를 다시 확인한다.
- 대화 초반에 확인한 에셋 정보만 믿고 작업하지 않는다. 사용자가 에셋을 이동, 교체, 삭제했을 가능성을 항상 고려한다.
- Animator Controller 등에 guid를 반영하기 전에는 반드시 해당 `.meta` 파일로 guid를 검증한다.

## 응답 규칙

- 작업 결과를 설명할 때는 변경 사항, 영향 범위, 검증 결과를 간단히 정리한다.
- 버그 수정 응답에서는 원인과 수정 내용을 구분해서 설명한다.
